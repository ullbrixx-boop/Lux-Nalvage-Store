{% liquid
  assign featured_tag = 'featured'
  assign new_tag = 'new'
  assign product_image = product.featured_image | default: product.images.first
%}
<article class="product-card">
  <a href="{{ product.url }}" class="product-card__media">
    {% if product_image != blank %}
      {{ product_image | image_url: width: 900 | image_tag: loading: 'lazy', alt: product_image.alt | default: product.title }}
    {% else %}
      {{ 'product-1' | placeholder_svg_tag }}
    {% endif %}
    <div class="product-card__badges">
      {% if product.tags contains new_tag %}
        <span class="badge badge--dark">Nuevo</span>
      {% endif %}
      {% if product.tags contains featured_tag %}
        <span class="badge badge--gold">Destacado</span>
      {% endif %}
      {% if product.compare_at_price > product.price %}
        <span class="badge badge--soft">Oferta</span>
      {% endif %}
    </div>
    <span class="product-card__quick-cta">Ver producto</span>
  </a>
  <div class="product-card__content">
    <div class="meta-row product-card__header" style="align-items:flex-start;">
      <div class="product-card__copy" style="min-width:0;">
        <a href="{{ product.url }}"><h3 class="product-card__title">{{ product.title }}</h3></a>
        {% if product.metafields.custom.subtitle != blank %}
          <div class="text-muted product-card__subtitle" style="margin-top:4px;font-size:0.8rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">{{ product.metafields.custom.subtitle }}</div>
        {% endif %}
      </div>
      <button type="button" class="icon-button product-card__action" aria-label="Agregar a favoritos">
        {% render 'icon', icon: 'heart' %}
      </button>
    </div>
    <div class="product-card__price-row" style="margin-top:18px;">
      <div>
        <div class="product-card__price" style="font-weight:700;">{{ product.price | money }}</div>
        {% if product.compare_at_price > product.price %}
          <div class="product-card__price-compare">{{ product.compare_at_price | money }}</div>
        {% endif %}
      </div>
      <a class="text-link product-card__link" href="{{ product.url }}" style="font-size:0.75rem;font-weight:700;">Comprar →</a>
    </div>
  </div>
</article>
