<section class="section page-shell page-shell--light" {{ section.shopify_attributes }}>
  <div class="container--narrow">
    <div class="page-section article-card">
      <div class="eyebrow">Página</div>
      <h1 class="heading-lg" style="margin-top:10px;">{{ page.title }}</h1>
      <div class="rte" style="margin-top:20px;">{{ page.content }}</div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main page",
  "settings": []
}
{% endschema %}
